import { SearchPlayersPipe } from './search-players.pipe';

describe('SearchPlayersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPlayersPipe();
    expect(pipe).toBeTruthy();
  });
});
